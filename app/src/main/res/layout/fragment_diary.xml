<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".Main.Fragment.DiaryFragment">

    <!-- TODO: Update blank fragment layout -->

    <!--<SearchView-->
        <!--android:id="@+id/sv_diary"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:iconifiedByDefault="false"-->
        <!--android:queryHint="Tìm nhật ký"/>-->

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rcv_diary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_diary">
    </android.support.v7.widget.RecyclerView>

    <TextView
        android:id="@+id/tv_no_diary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Chưa có nhật ký nào"
        android:padding="20dp"
        android:textSize="@dimen/font_size_header"
        android:textColor="@color/colorPrimary"
        android:layout_gravity="center"
        android:gravity="center"/>

</FrameLayout>